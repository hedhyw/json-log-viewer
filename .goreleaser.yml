version: 2

before:
  hooks:
    - make versioninfo VERSION={{ .Version }}

builds:
  - binary: jlv
    main: ./cmd/jlv
    goos:
      - windows
      - darwin
      - linux
    goarch:
      - "386"
      - amd64
      - arm
      - arm64
    goarm:
      - "6"
    ignore:
      - goos: darwin
        goarch: "386"
    env:
      - CGO_ENABLED=0
    # NOTE: do not add `-s -w` here. Stripping symbols and DWARF from
    # cross-compiled Windows binaries is a known trigger for Microsoft
    # Defender false positives (Trojan:Win32/Wacatac). See #175.
    ldflags:
      - -X main.version={{ .Version }}

gomod:
  proxy: false

archives:
  - name_template: "{{.Binary}}_{{.Os}}_{{.Arch}}"
    files:
      - README.md
      - LICENSE
    format_overrides:
      - goos: windows
        format: zip

release:
  draft: false

snapshot:
  version_template: "{{.Tag}}"

checksum:
  name_template: "task_checksums.txt"

nfpms:
  - vendor: Task
    homepage: https://github.com/hedhyw/jlv
    maintainer: Maksym Kryvchun <hedhyw@yahoo.com>
    description: JSON Log Viewer tool.
    license: MIT
    formats:
      - deb
      - rpm
    file_name_template: "{{.ProjectName}}_{{.Os}}_{{.Arch}}"

brews:
  - name: jlv
    description: JSON Log Viewer tool.
    license: MIT
    homepage: https://github.com/hedhyw/homebrews
    repository:
      owner: hedhyw
      name: homebrew-main
      token: "{{ .Env.HOMEBREW_TAP_GITHUB_TOKEN }}"
    test: system "#{bin}/jlv", "-help"
    install: |-
      bin.install "jlv"
